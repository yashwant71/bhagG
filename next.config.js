/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Enable CSS modules and global CSS
  // CSS files are already set up correctly
}

module.exports = nextConfig
